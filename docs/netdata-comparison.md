# Netdata comparison plan

No benchmark results are claimed yet. Measure the same idle and representative workload windows under:

## A. Netdata only

CPU overhead, RSS/RAM, disk writes, storage growth, temperature, and metric count.

## B. PiMonitoring only

CPU overhead, RSS/RAM, disk writes, storage growth, temperature, and metric count.

## C. Netdata + PiMonitoring

CPU overhead, RSS/RAM, disk writes, storage growth, temperature, and metric count.

Keep workload, sampling window, boot state, and filesystem conditions identical. Netdata remains outside this repository and is not changed by deployment.
